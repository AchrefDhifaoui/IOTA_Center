{% extends 'base.html.twig' %}

{% block title %}RS{% endblock %}

{% block body %}
    <h1>RS</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ r.id }}</td>
            </tr>
            <tr>
                <th>Taux</th>
                <td>{{ r.taux }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_r_s_index') }}">back to list</a>

    <a href="{{ path('app_r_s_edit', {'id': r.id}) }}">edit</a>

    {{ include('rs/_delete_form.html.twig') }}
{% endblock %}
