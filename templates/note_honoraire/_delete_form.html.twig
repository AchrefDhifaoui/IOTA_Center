<form method="post" action="{{ path('app_note_honoraire_delete', {'id': note_honoraire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ note_honoraire.id) }}">
    <button class="btn">Delete</button>
</form>
