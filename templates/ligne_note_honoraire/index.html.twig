{% extends 'base.html.twig' %}

{% block title %}LigneNoteHonoraire index{% endblock %}

{% block body %}
    <h1>LigneNoteHonoraire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Qantite</th>
                <th>PrixUnitaire</th>
                <th>PrixTotalHT</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ligne_note_honoraire in ligne_note_honoraires %}
            <tr>
                <td>{{ ligne_note_honoraire.id }}</td>
                <td>{{ ligne_note_honoraire.qantite }}</td>
                <td>{{ ligne_note_honoraire.prixUnitaire }}</td>
                <td>{{ ligne_note_honoraire.prixTotalHT }}</td>
                <td>
                    <a href="{{ path('app_ligne_note_honoraire_show', {'id': ligne_note_honoraire.id}) }}">show</a>
                    <a href="{{ path('app_ligne_note_honoraire_edit', {'id': ligne_note_honoraire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ligne_note_honoraire_new') }}">Create new</a>
{% endblock %}
